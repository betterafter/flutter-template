name: data
description: "A new Flutter package project."
publish_to: none
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build_runner: ^2.4.6
  source_gen: ^1.4.0

  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # 의존성 주입 라이브러리
  get_it: ^8.0.3

  injectable: ^2.5.0
  injectable_generator: ^2.5.0

flutter:
