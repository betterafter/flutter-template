name: data
description: "A new Flutter package project."
publish_to: none
version: 0.0.1
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  build_runner: ^2.6.0
  source_gen: ^4.0.0
  dio: ^5.7.0
  retrofit: ^4.7.0

  domain:
    path: ../domain

  injectable: ^2.5.0
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  injectable_generator: ^2.5.0
  retrofit_generator: ^10.0.0
  build: ^4.0.0
  glob: ^2.1.2

flutter:
