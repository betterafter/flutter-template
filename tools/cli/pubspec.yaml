name: flutter_clean_arch_cli
description: Flutter Clean Architecture 프로젝트 스캐폴딩 CLI
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.3

dependencies:
  args: ^2.6.0
  path: ^1.9.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.1

executables:
  flutter_clean_arch: flutter_clean_arch

dev_dependencies:
  lints: ^5.0.0
