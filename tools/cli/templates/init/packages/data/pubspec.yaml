name: data
description: Data layer for Clean Architecture.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: ../domain
  dio: ^5.7.0
  retrofit: ^4.7.0
  injectable: ^2.5.0
  get_it: ^8.0.3
  build_runner: ^2.6.0
  source_gen: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build: ^4.0.0
  glob: ^2.1.2
  injectable_generator: ^2.5.0
  retrofit_generator: ^10.0.0

flutter:
