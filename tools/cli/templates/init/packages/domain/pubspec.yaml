name: domain
description: Domain layer for Clean Architecture.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  injectable: ^2.5.0
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  build: ^2.4.0
  glob: ^2.1.2
  source_gen: ^1.4.0
  injectable_generator: ^2.5.0

flutter:
